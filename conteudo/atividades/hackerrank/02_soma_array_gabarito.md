# Gabarito - Soma Simples de Array (Simple Array Sum)

```python
def soma_array(arr):
    return sum(arr)

# Leitura de entrada
n = int(input())
arr = list(map(int, input().split()))
# Chamada da função e impressão do resultado
print(soma_array(arr))
``` 