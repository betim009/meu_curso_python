# Exercício 01 - Cliente com `__str__`

Crie uma classe `Cliente`.

## Requisitos

- Atributos: `nome` e `email`.
- Crie o método especial `__str__`.
- Ao usar `print(cliente)`, deve aparecer uma frase amigável com nome e e-mail.

## Exemplo esperado

```text
Ana Souza (ana@email.com)
```
